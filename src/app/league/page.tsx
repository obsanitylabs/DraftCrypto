import { PageShell } from '@/components/layout';
import { LeagueLobby } from '@/components/league';

export default function LeaguePage() {
  return (
    <PageShell>
      <LeagueLobby />
    </PageShell>
  );
}
